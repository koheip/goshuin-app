# このファイルを .env にコピーして値を入れる（.env は Git に入れない）

# 神社・お寺の追加画面の「Google で探す」（Places API (New)）。
# アプリに埋め込まれるので、Google Cloud で「Places API (New) だけ」「このアプリだけ」に制限すること
EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=

# Android のアプリ内地図（Maps SDK for Android）。開発ビルド・ストア版のビルド時に使う
GOOGLE_MAPS_ANDROID_API_KEY=
