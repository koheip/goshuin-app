# やることリスト

おすすめの順番：1 → 2 → 4 → 5 → 6 → 7

## 🔴 今すぐ（確認・決めること）

- [ ] **1. 実機で見た目を確認する**：パステル調・鳥居・しめ縄・桜が、スマホで読みやすく表示されるか確認し、調整する
  - [ ] ホーム右上のバックアップボタンで、見出しが窮屈になっていないか
  - [ ] 詳細画面の「地図で見る」で Google マップが開くか
  - [ ] 「記録を編集」で直した内容が、詳細画面と御朱印帳に反映されるか
  - [ ] バックアップを書き出し、そのファイルから戻せるか
- [ ] **2. ストア用の識別子を確定する**：今は仮に `com.koheip.goshuin`（[app.json](app.json)）。ストアに出したあとは変えられない
- [ ] **3. codex 用の作業フォルダを片付ける**：`../goshuin-app-codex` の worktree と `codex/release-prep` ブランチは使っていない

## 🟡 機能の追加

- [ ] **4. 御朱印帳を複数冊にする**：次の冊に移ったり、名前を変えたりできるようにする。DB には `books` テーブルが用意済み
- [ ] **5. 御朱印を並べ替える**：見開きで並ぶ順番を入れ替える。4 と同じ帳面の画面を触るので続けて行う
- [ ] **6. お寺にも対応する**：神社かお寺かを選べるようにする。DB の `shrines.kind` は用意済み
- [ ] **7. データベースの操作のテストを追加する**：4〜6 で変わる帳面まわりの処理を守る（日付と地図URLのテストは追加済み）

## 🟢 公開に向けた準備

- [ ] **8. アプリのアイコンとスプラッシュ画像を作る**：今は Expo のひな形のまま
- [ ] **9. プライバシーポリシーを作る**：カメラと写真ライブラリを使うため必要。Google マップと連携する場合は、そのことも書く
- [ ] **10. 開発ビルドを用意する**：`expo-dev-client` を追加し、`eas build --profile development` でビルドする。入れると `npx expo start` の既定が Expo Go 向けではなくなる点に注意

## 🗺️ Google マップ連携（余裕があれば）

データベースの神社の項目に、住所・緯度・経度・`place_id` の保存場所は用意済み。「地図で見る」は緯度・経度や Place ID があればそれを使う。

- [ ] **11. Google で神社を検索して登録（方法B）**：名前・住所・位置・Place ID をまとめて入れる
  - [ ] Google Cloud で APIキーを作り、課金を設定する
  - [ ] APIキーの使い道を制限して、他人に使われないようにする
  - [ ] 保存するのは Place ID だけにする（Google の利用規約のため）
- [ ] **12. アプリ内に地図を表示（方法C）**：参拝した神社を地図上にピンで並べる。10 の開発ビルドが必要

## ⚪ 気にしておくこと

- **バックアップの大きさ**：今は写真を base64 で1つの JSON に入れている。写真が数百枚になると書き出し・読み込みが重くなるので、その頃に分けて保存する方式を検討する

## ✅ 完了

- [x] 検索欄の色の変更漏れを直す
- [x] データのバックアップ機能を作る（ホーム右上 → バックアップ）
- [x] 記録を編集できるようにする
- [x] 「地図で見る」ボタン（方法A、APIキー不要）
- [x] ストア用の識別子を設定する（仮の値。2 で確定する）
- [x] EAS の設定（[eas.json](eas.json)）
- [x] 使っていないフォントのパッケージを削除する
- [x] 手元のブランチ名を `main` にする
- [x] テストの仕組みを入れる（`npm test`、日付と地図URL）
